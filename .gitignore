# --- Build / output artifacts ---
/build/out/
*.pdf
/build/specimen/*.png

# --- Python virtual environment (PDF pipeline: Playwright) ---
.venv/

# --- Node (for the future PDF pipeline) ---
node_modules/
package-lock.json
npm-debug.log*

# --- Editor / OS cruft ---
.DS_Store
Thumbs.db
*~
*.tmp

# --- Claude Code local settings (machine-specific) ---
.claude/settings.local.json

# --- Claude Code per-session worktrees (if ever enabled) ---
# Not used today (parallel sessions share the main checkout); ignored so the
# isolated copies never show up as untracked files in the main folder.
.claude/worktrees/
